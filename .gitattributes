# Normalize line endings: always LF on checkout (all platforms)
# Overrides core.autocrlf=true on Windows runners
* text=auto eol=lf

# Binary assets (never touch line endings)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
